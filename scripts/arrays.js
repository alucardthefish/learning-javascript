
const numbers = new Array(1, 2, 3, 4, 5);
const fruits = ['apples', 'oranges', 'pears'];

console.log(numbers);

// Accessing values
console.log(fruits[1]);

// Seting values

fruits[3] = 'grapes'; // Set at specific index

fruits.push('mangos'); // Set at the end of array
fruits.unshift('strawberrys'); // Set at the start of array

console.log(fruits);
